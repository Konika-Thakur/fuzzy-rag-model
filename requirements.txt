# Core dependencies for Enhanced Fuzzy RAG Product Feature Assistant
streamlit>=1.28.0
requests>=2.31.0
numpy>=1.24.0

# Optional dependencies (install as needed)
# For embeddings (if you want vector similarity):
# sentence-transformers>=2.2.0
# qdrant-client>=1.6.0

# For LLM providers (choose one):
# openai>=1.0.0  # For OpenAI
# anthropic>=0.7.0  # For Anthropic Claude

# The system works without these optional dependencies using fallback methods